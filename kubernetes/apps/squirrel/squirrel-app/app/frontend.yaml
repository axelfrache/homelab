apiVersion: apps/v1
kind: Deployment
metadata:
  name: squirrel-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: squirrel-frontend
  template:
    metadata:
      labels:
        app: squirrel-frontend
    spec:
      containers:
        - name: frontend
          image: ghcr.io/axelfrache/squirrel-frontend:latest@sha256:ba9370b6f7c38b238c37b8235abf4d687505c9bb59268f6d2836cf7a6267c94d # {"$imagepolicy": "flux-system:squirrel-frontend"}
          ports:
            - name: http
              containerPort: 80
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              memory: 64Mi
          readinessProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 10
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: squirrel-frontend
spec:
  selector:
    app: squirrel-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
