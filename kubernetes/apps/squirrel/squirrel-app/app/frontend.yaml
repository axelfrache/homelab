apiVersion: apps/v1
kind: Deployment
metadata:
  name: squirrel-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: squirrel-frontend
  template:
    metadata:
      labels:
        app: squirrel-frontend
    spec:
      containers:
        - name: frontend
          image: ghcr.io/axelfrache/squirrel-frontend:latest@sha256:ffc001015dcd515d64a414d1e127e03113d4f3c3560139095ce93c463f30ba8c # {"$imagepolicy": "flux-system:squirrel-frontend"}
          ports:
            - name: http
              containerPort: 80
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              memory: 64Mi
          readinessProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 10
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: squirrel-frontend
spec:
  selector:
    app: squirrel-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
